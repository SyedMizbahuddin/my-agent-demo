# Development

## Prerequisites

- Install a supported language/runtime (e.g., Node.js/Python) once the project contains code.
- Ensure you have Git and an editor/IDE.

## Local setup

> Replace placeholders with the actual steps once the project has dependencies.

1. Clone the repo
2. Create a virtual environment (if Python) or install dependencies (if Node)
3. Configure environment variables (see `docs/configuration.md`)

## Running locally

> Add the exact commands once available.

- Test: `...`
- Lint: `...`
- Start: `...`

## Suggested dev workflow

- Use a feature branch
- Add/adjust tests
- Update docs for user-facing changes
- Ensure CI passes

## Troubleshooting

List common issues (dependency installation, auth errors, ports, etc.) and their fixes.
